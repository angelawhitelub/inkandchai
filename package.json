{
  "name": "akshar-co",
  "version": "1.0.0",
  "description": "Akshar & Co. — Fine Books",
  "scripts": {
    "dev": "netlify dev",
    "build": "python3 generate_site.py",
    "routes": "node scripts/build-worker-routes.js",
    "stamp": "node scripts/stamp-build-info.js",
    "deploy": "npm run routes && npm run stamp && npx wrangler deploy",
    "test": "node --test netlify/functions/utils/*.test.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@netlify/blobs": "^11.0.1",
    "@simplewebauthn/server": "^13.1.1",
    "@supabase/supabase-js": "^2.43.4",
    "razorpay": "^2.9.2"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.658.0"
  }
}
